[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg"]

[phases.install]
cmds = ["npm install --no-package-lock"]

[phases.build]
cmds = []

[start]
cmd = "node scripts/auto-setup-on-deploy.js && node backend/server.js"
